package com.proiect.cofetarie.mapper;

import com.proiect.cofetarie.dto.order.CreateNewOrderDTO;
import com.proiect.cofetarie.model.Order;
import org.aspectj.weaver.ast.Or;
import org.springframework.stereotype.Component;

@Component
public class OrderMapper {
//    public Order create(CreateNewOrderDTO request) {
//        return new Order(request.getTotalPrice(), request.getOrderDate(), request.getDeliveryDate(), false, request.getClientID(), request.getCakesIDList());
//    }
//    public Order update(UpdateDeliveryAddressDTO request) {
//        return new DeliveryAddress(request.getId(), request.getCountry(), request.getRegion(), request.getCity(), request.getStreet(), request.getStreetNumber(), request.getFloorNumber(), request.getApartmentNumber());
//    }
}
